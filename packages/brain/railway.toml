[build]
dockerfilePath = "packages/brain/Dockerfile"

[deploy]
startCommand = "uvicorn jarvis.inference.server:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/internal/brain/health"
healthcheckTimeout = 30

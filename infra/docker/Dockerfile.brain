FROM python:3.11-slim

WORKDIR /app

COPY packages/brain/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY packages/brain/ .

ENV PYTHONPATH=/app
ENV BRAIN_BACKEND=ollama

EXPOSE 8000

CMD ["uvicorn", "jarvis.inference.server:app", "--host", "0.0.0.0", "--port", "8000"]
